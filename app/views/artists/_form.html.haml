= form_for @artist do |f|
  %ul
    - @artist.errors.full_messages.each do |error|
      %li= error

  %p
    = f.label :name
    = f.text_field :name
  %p
    = f.label :image_path
    = f.text_field :image_path
  %p= f.submit
